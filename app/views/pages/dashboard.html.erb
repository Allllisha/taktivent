<div class="background-pages-dashboard">
  <br>
  <div class="container">
    <% if user_signed_in? %>
      <h3> Hello <%= current_user.first_name %> 👋 </h3>
    <% end %>
  </div>

  <div class="container">
    <br>

    <div class="card-category-dashboard" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2))">

    </div>

    <br>

    <div class="cards">

      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1488630228244-bcdf33562a43?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1632&q=80)">
        <%= pluralize @events.count, "upcoming event" %>
      </div>

      <div class="card-category-dashboard" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2))">

      </div>

      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(https://images.unsplash.com/photo-1603741209938-50fc882da856?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1470&q=80)">
        <% if @event_reviews.blank? %>
          No review yet
        <% else %>
          <% @events_reviews.last(1).each do |review| %>
          <p><%= review.content %></p>
          <% end %>
        <% end %>
      </div>

    </div>
  </div>

  <div class="container mt-5">
    <h5>Manage my events</h5>
      <div class="d-flex flex-column">
          <% @events.each do |event| %>
            <% if event.images.attached? %>
              <div class="card-product-dashboard" style="background-image: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(<%= cl_image_path(event.images.first.key) %>)">
            <% end %>
            <a href="<%= manage_event_path(event)%>"><%= event.name %></a>
              <div class="container">
                <% event.songs.first(3).each do |song| %>
                  <div>
                    <%= song.name %>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
      </div>
  </div>
  <br>
</div>
